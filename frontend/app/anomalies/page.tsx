import {api} from "@/lib/api";import type {Anomaly} from "@/lib/types";import {AnomalyTable,PageHead} from "@/components/data-pages";export default async function Page(){const data=await api<Anomaly[]>("/api/anomalies").catch(()=>[]);return <div className="p-7"><PageHead title="Anomaly explorer" sub="Ranked deviations across every operational signal"/><AnomalyTable items={data}/></div>}
