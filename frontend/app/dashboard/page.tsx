import {api,fallback} from "@/lib/api";import type {Analytics,Service} from "@/lib/types";import {Dashboard} from "@/components/dashboard-client";export default async function Page(){const [analytics,services]=await Promise.all([api<Analytics>("/api/analytics").catch(()=>fallback.analytics),api<Service[]>("/api/services").catch(()=>fallback.services)]);return <Dashboard analytics={analytics} services={services}/>}
