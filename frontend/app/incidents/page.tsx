import {api} from "@/lib/api";import type {Incident} from "@/lib/types";import {IncidentTable,PageHead} from "@/components/data-pages";export default async function Page(){const data=await api<Incident[]>("/api/incidents").catch(()=>[]);return <div className="p-7"><PageHead title="Incidents" sub="Investigations, evidence, and resolution activity"/><IncidentTable items={data}/></div>}
