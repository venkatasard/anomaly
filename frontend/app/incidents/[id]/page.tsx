import {api} from "@/lib/api";import {PageHead} from "@/components/data-pages";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const d=await api<any>(`/api/incidents/${id}`).catch(()=>null);if(!d)notFound();const summary=d.summaries?.[0];return <div className="p-7"><PageHead title={d.incident.title} sub={`${d.service.display_name} · ${d.incident.status}`}/><div className="grid grid-cols-[1fr_380px] gap-4"><div className="space-y-4"><section className="panel p-5"><div className="label">Incident timeline</div><div className="mt-5 border-l border-line pl-5"><div className="relative pb-6 text-sm before:absolute before:-left-[25px] before:top-1 before:h-2 before:w-2 before:rounded-full before:bg-orange-400"><b>Anomaly detected</b><p className="mt-1 text-muted">{new Date(d.incident.opened_at).toLocaleString()}</p></div>{d.notes.map((n:any)=><div className="relative pb-6 text-sm before:absolute before:-left-[25px] before:top-1 before:h-2 before:w-2 before:rounded-full before:bg-cyan" key={n.id}><b>{n.author} added a note</b><p className="mt-1 text-muted">{n.body}</p></div>)}</div></section><section className="panel p-5"><div className="label">Related logs</div>{d.logs.map((l:any)=><div className="border-b border-line py-3 font-mono text-xs" key={l.id}><span className="mr-3 text-orange-400">{l.level}</span>{l.message}</div>)}</section></div><aside className="panel h-fit p-5"><div className="label text-brand">AI investigation</div>{summary?<><h3 className="mt-4 font-medium">Situation</h3><p className="mt-2 text-sm leading-6 text-muted">{summary.summary}</p><h3 className="mt-5 font-medium">Likely root cause</h3><p className="mt-2 text-sm leading-6 text-muted">{summary.root_cause}</p><h3 className="mt-5 font-medium">Recommended actions</h3><ol className="mt-2 list-decimal space-y-2 pl-4 text-sm text-muted">{summary.recommended_actions.map((a:string)=><li key={a}>{a}</li>)}</ol><div className="mt-5 border-t border-line pt-4 text-xs text-muted">Confidence <span className="float-right font-mono text-brand">{(summary.confidence*100).toFixed(0)}%</span></div></>:<p className="mt-4 text-sm text-muted">Analysis is being generated.</p>}</aside></div></div>}
