import {api,fallback} from "@/lib/api";import type {Service} from "@/lib/types";import {PageHead,Services} from "@/components/data-pages";export default async function Page(){const data=await api<Service[]>("/api/services").catch(()=>fallback.services);return <div className="p-7"><PageHead title="Service catalog" sub="Ownership and live health across the production estate"/><Services items={data}/></div>}
