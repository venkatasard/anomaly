"use client";import Link from "next/link";import {usePathname} from "next/navigation";import {Activity,AlertTriangle,BarChart3,Box,Command,LayoutDashboard,Radio,Search,Settings,Waypoints} from "lucide-react";import {useLiveStore} from "@/lib/store";import {Realtime} from "./realtime";
const links=[["/dashboard",LayoutDashboard,"Overview"],["/anomalies",AlertTriangle,"Anomalies"],["/incidents",Activity,"Incidents"],["/streams",Waypoints,"Streams"],["/search",Search,"Explore"],["/services",Box,"Services"],["/analytics",BarChart3,"Analytics"]] as const;
export function Shell({children}:{children:React.ReactNode}){const path=usePathname();const connected=useLiveStore(s=>s.connected);return <div className="min-h-screen bg-bg"><Realtime/><aside className="fixed inset-y-0 left-0 z-20 w-60 border-r border-line bg-[#090d13]"><div className="flex h-16 items-center gap-3 border-b border-line px-5"><div className="grid h-8 w-8 place-items-center rounded-lg bg-brand text-black"><Command size={18}/></div><b className="tracking-tight">ANOMALY</b><span className="ml-auto rounded bg-white/5 px-1.5 py-0.5 text-[9px] text-muted">OPS</span></div><nav className="space-y-1 p-3">{links.map(([href,Icon,label])=><Link key={href} href={href} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${path.startsWith(href)?"bg-brand/10 text-brand":"text-muted hover:bg-white/5 hover:text-white"}`}><Icon size={16}/>{label}</Link>)}</nav><div className="absolute bottom-0 w-full border-t border-line p-4"><div className="mb-3 flex items-center gap-2 text-xs text-muted"><Radio size={13} className={connected?"text-brand":"text-red-400"}/>{connected?"Live stream connected":"Reconnecting stream"}</div><button className="flex items-center gap-2 text-sm text-muted"><Settings size={15}/>Settings</button></div></aside><main className="ml-60 min-h-screen"><header className="sticky top-0 z-10 flex h-16 items-center border-b border-line bg-bg/80 px-7 backdrop-blur"><div><div className="label">Operations workspace</div><div className="text-sm">Production / All regions</div></div><div className="ml-auto flex items-center gap-3"><button className="btn flex items-center gap-2"><Search size={14}/>Search <kbd className="text-[10px] text-muted">⌘K</kbd></button><div className="h-8 w-8 rounded-full bg-gradient-to-br from-cyan to-brand"/></div></header>{children}</main></div>}
